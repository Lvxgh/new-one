# AI Research Agent v0.1 起步说明

当前项目结构可以作为 v0.1 起步版，先不要急着拆得太复杂。这个阶段的重点是把 `arxiv_search.py`、`main.py` 和 `data/raw_papers.json` 这条最小流程跑稳。

## 当前目录职责

```text
AGENT/
├── data/
│   └── 用来保存程序生成的数据
├── arxiv_search.py
│   └── 专门负责 arXiv 论文检索
├── main.py
│   └── 项目入口，用来调用各个模块测试流程
└── README.md
    └── 项目说明文档
```

## 1. data/ 文件夹

`data/` 用来保存程序运行后生成的数据。

后面可能会生成：

```text
data/
├── raw_papers.json
└── report.md
```

其中 `raw_papers.json` 用来保存 arXiv 搜到的原始论文数据，例如：

```json
[
  {
    "title": "xxx",
    "summary": "xxx",
    "authors": ["Alice", "Bob"],
    "published": "2024-01-01",
    "link": "http://arxiv.org/abs/xxxx"
  }
]
```

## 2. arxiv_search.py

`arxiv_search.py` 只负责一件事：

输入关键词，返回论文列表。

它主要提供功能，不要写太多 `print`。建议代码结构如下：

```python
import requests
import xml.etree.ElementTree as ET


def search_arxiv(query, max_results=5):
    url = "http://export.arxiv.org/api/query"

    params = {
        "search_query": f"all:{query}",
        "start": 0,
        "max_results": max_results
    }

    response = requests.get(url, params=params, timeout=10)

    root = ET.fromstring(response.text)
    namespace = "{http://www.w3.org/2005/Atom}"

    entries = root.findall(namespace + "entry")

    papers = []

    for entry in entries:
        title = entry.find(namespace + "title").text.strip()
        summary = entry.find(namespace + "summary").text.strip()
        published = entry.find(namespace + "published").text.strip()
        link = entry.find(namespace + "id").text.strip()

        authors = []
        for author in entry.findall(namespace + "author"):
            name = author.find(namespace + "name").text.strip()
            authors.append(name)

        paper = {
            "title": title,
            "summary": summary,
            "authors": authors,
            "published": published,
            "link": link
        }

        papers.append(paper)

    return papers
```

## 3. main.py

`main.py` 是项目入口，用来测试整体流程。

可以先让它负责：

- 设置检索关键词
- 调用 `search_arxiv`
- 在终端打印论文信息
- 把结果保存到 `data/raw_papers.json`

示例代码：

```python
import json
from arxiv_search import search_arxiv


def main():
    query = "multi-agent"

    papers = search_arxiv(query, max_results=5)

    for index, paper in enumerate(papers, start=1):
        print(f"\n论文 {index}")
        print("标题：", paper["title"])
        print("作者：", ", ".join(paper["authors"]))
        print("发布时间：", paper["published"])
        print("链接：", paper["link"])
        print("摘要：", paper["summary"][:300], "...")

    with open("data/raw_papers.json", "w", encoding="utf-8") as f:
        json.dump(papers, f, ensure_ascii=False, indent=2)

    print("\n论文数据已保存到 data/raw_papers.json")


if __name__ == "__main__":
    main()
```

现在可以把 `main.py` 理解成临时项目入口。后面项目复杂起来以后，它可能会慢慢被 `workflow.py` 或 `app.py` 替代。

## 4. README.md

`README.md` 现在先写最简单版本：

````markdown
# AI Research Agent

这是一个面向高校学生科研前期工作的 AI 科研智能体项目。

## 当前目标

v0.1 阶段目标：

- 输入研究关键词
- 调用 arXiv API 检索论文
- 提取论文标题、摘要、作者、发布时间和链接
- 保存为 JSON 数据

## 当前目录结构

```text
AGENT/
├── data/
├── arxiv_search.py
├── main.py
└── README.md
```

## 运行方式

```bash
python main.py
```

## 下一步计划

- 封装 LLM 调用模块
- 根据用户主题生成检索关键词
- 对论文进行相关性分析
- 生成初步综述框架
````

## 当前阶段完成标准

现在这个阶段只要做到：

```text
python main.py
↓
成功搜索论文
↓
终端显示论文信息
↓
data/raw_papers.json 生成成功
```

这就算第一个模块完成。

## 下一步再添加的模块

后续可以再逐步增加：

- `llm_client.py`
- `planner.py`
- `paper_analyzer.py`

但现在先别急，先把 arXiv 检索到保存 JSON 的最小链路跑通。
